1️⃣ docker build  = 	Create a image by docker file 
  EX: docker build  -t <Docker-file-name> .

2️⃣ docker run    =  	Start a new container on image 
   EX: docker run -d -p 5000:5000 <Docker-image-name>

3️⃣ docker ps     = 	See running containers
   { it will show the running containers } 

4️⃣ docker ps -a  = see the running and stoped containers
   { it will show the sunning and stoped containers}

5️⃣ docker stop	  =   Stop a container
    EX: docker stop <container_id>

6️⃣ docker rm	    =   Remove a container
    EX: docker stop <container_id>

7️⃣ docker images	  =  See saved images
   { to see how many images we have }

8️⃣ docker rmi    =	 Remove an image
    EX: docker rmi <image-name>

9️⃣docker pull   = to get image from docker rigesrty
  EX : docker pull < image-nmae>

🔟docker push = upload our image to docker rigestry
   EX : docker push myusername/image-name

1️⃣1️⃣ docker exec   = Run a command inside a running container
  EX : docker exec -it <container_id> bash


